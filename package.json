{
  "name": "folium-monorepo",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:client": "npm run dev -w client",
    "dev:server": "npm run dev -w server",
    "dev": "npm run dev:client & npm run dev:server",
    "test": "npm run test -w server",
    "build": "npm run build -w client"
  }
}
